#include "pch.h"
#include "iostream"
#include <math.h>
using namespace std;
int main()
{

	setlocale(LC_ALL, "Russian");
	double a, b, c, p, S;
	cout << "Введите длины сторон a, b, c:" << endl;
	cin >> a >> b >> c;
	if (a <= 0 || b <= 0 || c <= 0 || a > (b + c) || b > (a + c) || c > (a + b))
	{
		do	{
			cout << "Сторона треугольника не может быть навна 0 или отрицательному числу.\n";
			cout << "Одна сторона треугольника больше суммы двух других сторон.\n";
			cout << "Такой треугольник не может существовать.\n";
			cout << "Введите длины сторон a, b, c:" << endl;
			cin >> a >> b >> c;
		} while (a <= 0 || b <= 0 || c <= 0 || a > (b + c) || b > (a + c) || c > (a + b));
	}
	p = (a + b + c) / 2;
	S = pow(p * (p - a)*(p - b)*(p - c), 0.5);
	cout << "S= " << S << endl;
	system("pause");
	return 0;
}
